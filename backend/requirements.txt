fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
sqlalchemy>=2.0
psycopg[binary]>=3.1  # postgres driver, for LL_DATABASE_URL=postgresql+psycopg://...
pyjwt>=2.8
bcrypt>=4.0
numpy>=1.26
opencv-python-headless>=4.9
pillow>=10.0
pymupdf>=1.24
easyocr>=1.7
rapidfuzz>=3.6
strawberry-graphql>=0.230  # GraphQL integration surface (PS 26018 REST/GraphQL requirement)
# tests only (README's `pip install -r backend/requirements.txt` then `pytest tests` assumes
# these are here - a fresh checkout/CI has nothing else that would install them)
pytest>=8.0
httpx>=0.27  # fastapi.testclient.TestClient needs this
# dataset generator only
playwright>=1.45
