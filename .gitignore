# python
venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/

# env / secrets
.env
.env.*
!.env.example
*.pem
*.ppk
id_rsa*
id_ed25519*
*.pfx

# node
node_modules/
dist/

# model weights
*.pt
*.pth
*.onnx

# runtime data
uploads/
storage/
*.sqlite3
*.db
data/generated/
# simulated state register, rebuilt by scripts/seed_register.py
data/register.json
data/fonts/

# editor / os
.vscode/
.idea/
.DS_Store
Thumbs.db

# Real land records carry real people's names. This repository is public, so the documents and
# their typed ground truth stay on the machine that measured them; only the README and the
# aggregate results (eval/results/real.md, no names) are ever committed.
data/real/*
!data/real/README.md
